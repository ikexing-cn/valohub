{
  "name": "@valorant-bot/bot",
  "private": true,
  "type": "module",
  "files": [
    "dist",
    "node_modules",
    "ecosystem.config.cjs",
    "safe-pm2-control.cjs"
  ],
  "scripts": {
    "dev": "esno watch --clear-screen=false src/main",
    "deploy": "rimraf .deploy && pnpm --config.shamefully-hoist=true --config.shared-workspace-lockfile=false --filter bot deploy .deploy --prod --no-optional"
  },
  "dependencies": {
    "@valorant-bot/core": "workspace:^",
    "@valorant-bot/shared": "workspace:^",
    "dotenv": "^16.3.1",
    "go-cqwebsocket": "^6.2.14",
    "puppeteer": "^21.7.0"
  }
}
